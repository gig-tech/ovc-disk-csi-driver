# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
main
cmd

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# IDE
.vscode
.idea/

# prevent env files from being pushed
*.env

# prevent tmp playbook configuration files from being pushed
tmp/
